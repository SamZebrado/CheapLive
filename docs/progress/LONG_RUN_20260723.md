# CheapLive Long Run — 2026-07-23

## Current phase

Phase 3 closure: core recovery/stability gates remain closed; upper-body motion Beta is implemented and verified within the explicit device boundary below.

## Commands and results

- `df -h /`: PASS; 16 GiB available, full execution tier.
- Recovery branch identity: PASS; clean `codex/recovery-organize-motion-mvp-20260722` at `6d906be2d4679a3457628492382d22e1680ba68e`.
- `git fetch origin --prune` and `git fetch gitee --prune`: PASS.
- Remote recovery branches: PASS on both remotes.
- GitHub `main` and Gitee `master`: both still at `d4a8222e3eb742d0e6b0a936a958035b4e4183e9`.
- Created `codex/core-stability-motion-beta-20260723` in the existing worktree with parent `6d906be`.
- Reviewed the four recovery commits and current mesh implementation. The 28-triangle fluke contract is implemented explicitly as 24 nose triangles, two fluke planes, and two body connectors; existing non-degeneracy and index checks support the corrected assertions.
- `ASSET_SYNC_GATE`: PASS. Eleven canonical groups passed check/sync/check/sync/check; both sync runs copied zero files. Manifest SHA256 remained `46a0b3ffa86cf78cb99dc31227a0f921948b52cc2dead4dd28b5577413afe102`.
- Asset checks passed from the repository root, `scripts/`, the repository parent, and `/tmp`.
- Asset safety tests: 11/11 PASS, covering path escape, missing source/target, stale target/manifest, symlink escape, CWD independence, and second-sync idempotence.
- `npm ci --prefer-offline --no-audit --no-fund`: PASS after using a writable temporary npm cache and network approval; 52 lockfile-pinned packages installed. Package and lockfile were not changed.
- Complete non-signaling Node suite initially exposed two tests that the old `*.test.js` glob skipped: a missing `node:test` import and neutral spindle deformation. Both root causes were fixed; 358/358 unit tests now pass.
- Signaling was converted to an import-safe factory using `127.0.0.1` and dynamic ports in tests. It now closes sockets/timers deterministically, supports room isolation and optional token auth, rejects invalid/non-finite payloads and stale/duplicate sequences, and preserves token-free legacy mode when no server token is configured.
- Signaling tests: 13/13 PASS, including two clients, room isolation, token rejection, delivery, disconnect/reconnect cleanup, ordering, concurrent ports, and port release after stop.
- Playwright now reserves a dynamic loopback port, starts a scoped static server, writes JSON/trace failure evidence under `.tmp/test-evidence/20260723`, and stops the server after the run. The old fixed-port voice UI tests now use the configured base URL.
- The canonical browser gate found and fixed two Android contest-asset paths that pointed at a nonexistent `../face-tracking/` directory, plus a duplicate `const mapped` declaration that prevented the bundled page from parsing.
- Receiver runtime hardening: one sanitized face-frame acceptance path rejects non-finite, invalid-sequence, duplicate, and stale frames; SSE polling fallback now retries SSE with bounded backoff; WebSocket retry, SSE, polling, and audio resources are released on page exit; token values are no longer written to the on-page WebSocket log.
- Capture runtime hardening: concurrent camera starts share a single in-flight request, stale camera resolutions release their tracks, repeated stop is safe, and page exit stops camera and voice resources.
- `WEB_E2E_GATE`: PASS, 32/32 Chromium desktop tests in 24.3 s. The selected suite covers primary entry points, local MediaPipe resources, visible rendering, frame validation/defaults/order, transparent app mode, audio release, SSE recovery, capture start/stop/page-exit lifecycle, subtitles, receiver frames, and voice UI.
- Post-test process inspection found no remaining CheapLive signaling, Playwright, Chromium, or static-server process.
- Android wrapper/toolchain: Gradle 8.4 with Android Studio JBR 17.0.7. The worktree has no machine-specific `local.properties`; build commands used the existing `/Users/samzebrado/Library/Android/sdk` through process-local environment variables.
- The first Android unit compile exposed a stale positional `CaptureBridge` constructor call. After converting it to named arguments, three protocol tests reached an unmocked `android.util.Log` JVM stub. Bridge logging is now failure-safe rather than enabling global Android stub defaults, so protocol assertions execute without suppressing unrelated framework mistakes.
- `ANDROID_BUILD_GATE`: PASS. `assembleDebug` and `testDebugUnitTest` completed successfully; 26/26 JVM tests passed with zero skips/failures. Debug APK: `android-capture/app/build/outputs/apk/debug/app-debug.apk`, 25 MiB, SHA256 `9b135713cc432342550266146d54e0596f60353729d5120433b4757778333fec`.
- Device core partial evidence: `adb install -r` PASS with app data retained; camera and microphone permissions were already granted; cold launch PASS; LocalServer Receiver returned HTTP 200 and an invalid token returned HTTP 403.
- Camera stress: ten stop/start cycles plus five camera/audio alternation cycles completed with zero permission rejection or fatal event. Android camera service showed exactly one active CheapLive client after the test. Audio stress: ten start/stop cycles completed; the final UI was disabled and the last system recording event was a stop.
- Background test: after Home, LocalServer still returned HTTP 200. During the screen-off test the USB ADB transport itself became `offline`; shell commands and ADB forwarding stopped responding, so lock/unlock behavior is not yet verified and `DEVICE_CORE_GATE` remains open.
- Source audit found LocalServer was Activity-owned with no foreground lifetime owner. `CaptureServerService` now provides an Android 14 `connectedDevice` foreground-service owner and explicit notification stop action; `CaptureServerRuntime` and `IdempotentResource` enforce one server across Activity recreation, stop/restart release, and no duplicate bind. Camera/microphone remain Activity-owned and are released on Activity destruction.
- Rebuilt after service integration: 28/28 Android JVM tests PASS and merged manifest contains the required foreground-service permissions/type.
- Receiver transport control is now a canonical shared asset with a deterministic Android mirror (asset groups: 12). It enforces one retry timer per channel, exponential backoff with jitter and a 30 s cap, online/offline and visibility pause/resume, polling fallback with failure backoff, periodic preferred-SSE recovery, and terminal unauthorized/stopped states.
- Receiver transport regression: Node 360/360 PASS; Chromium 33/33 PASS, including SSE failure→polling→SSE, offline→online recovery, invalid-token retry suppression, WebSocket/frame checks, and page-exit cleanup. Post-run process inspection found no browser or test-server residue.
- Device lifecycle continuation: Activity background/return, screen off/on, lock/unlock, Activity recreation, explicit service stop/restart, and brief Wi-Fi loss/recovery all retained or restored Receiver HTTP 200 as expected. Exactly one foreground service remained after recreation. The persisted connection identity survived restart and `adb install -r`; a deliberate identity reset invalidated the old credential and restored the service with the replacement identity.
- Process-death boundary: a direct process kill on this Xiaomi build removed the foreground service and Receiver availability despite `START_STICKY`. Reopening CheapLive reconstructed the service from persisted identity. This is an explicit offline/reopen state, not a claim of automatic OEM restart.
- Final transparent-overlay verification used the existing TransparentFloatingBrowser installation and the final APK asset: avatar-only app mode had a transparent background while avatar pixels remained opaque; EDIT and DISPLAY both rendered; DISPLAY carried `FLAG_NOT_TOUCHABLE`; a tap through avatar-covered pixels changed the underlying authenticated CheapLive state; no crash occurred. The temporary overlay was stopped after measurement.
- Final live-camera sample: permission granted, five non-empty video pixel samples, active MediaPipe frame loop, approximately 10.0 video inference FPS, no face detected in the unattended scene, and therefore no fabricated live face-publish FPS. Active PSS was 256,564 KiB; the coarse main-process CPU snapshot was 0.3%; Android UI rendering reported 15/460 janky frames (3.26%). Camera stop left zero active CheapLive camera clients. No FATAL or ANR was observed.
- Receiver face-frame arrival, monotonic sequence, stale rejection, tracking-lost/recovery, SSE fallback/recovery, WebSocket reconnect, and bounded retry behavior are covered by the deterministic browser gate. The live-device sample establishes the camera/video inference path but does not claim a human-face acquisition result for a scene in which no face was present.
- A full-gate run exposed a reconnect diagnostic race: an already-active SSE stream could deliver valid state while a concurrent transport left the shared state at `connecting-sse`. Every valid SSE state now reasserts `connected-sse`; the focused race test passed 5/5 consecutive runs.
- Final software closure: unit 360/360, asset safety 11/11 with 12 synchronized groups, signaling 13/13, Chromium 34/34, Android JVM 29/29 with zero skips/failures. The final `testDebugUnitTest assembleDebug --rerun-tasks` build passed all 43 tasks.
- Final debug APK: `android-capture/app/build/outputs/apk/debug/app-debug.apk`, 26,128,508 bytes, SHA256 `4943596cc0b01fe128294a5f22a0944b8e82ebe2a52be0fbad3da06536791598`. `adb install -r` succeeded, retained the connection identity, returned authenticated Receiver HTTP 200, emitted `Cache-Control: no-store`, and kept the foreground service active.
- Final process inspection found zero residual CheapLive Playwright, Chromium, signaling, or static-server test processes.
- Motion protocol: schema v1, nine-landmark allowlist, simulator, validator, revision/sequence/confidence gates, smoothing, calibration, mirror semantics, stale rejection, and tracking-loss neutral fallback are platform-neutral under `src/shared/`.
- Motion model: official MediaPipe Pose Landmarker Lite `float16/1`, 5,777,746 bytes, SHA256 `59929e1d1ee95287735ddd833b19cf4ac46d29bc7afddbbf6753c459690d574a`, Apache-2.0, pinned in the asset manifest and loaded only after explicit enable.
- MediaPipe Worker integration exposed two runtime constraints: the WASM loader needs classic-Worker `importScripts`, and a dynamic bootstrap must queue messages until its module owns `onmessage`. The final bootstrap preserves both constraints and the browser gate proves zero pose-model request while disabled followed by an explicit-enable model request.
- Capture integration reuses the same video stream for face and pose, transfers resized `ImageBitmap` frames, allows one pose inference at a time, prevents face inference while the pose Worker is busy, and releases Worker/bitmaps on disable/page exit. Low/balanced/high caps are face/pose 15/8, 20/12, and 30/20 FPS.
- Android bridge accepts only schema v1 pose metadata under 32 KiB, finite bounded values, and the nine known landmarks. It rebroadcasts a sanitized envelope, persists settings only, throttles aggregate telemetry, and keeps model readiness separate from tracking state.
- Receiver integration owns an independent `PoseState`, aspect correction, confidence-aware smoothing, derived upper-body angles, tracking timeout, neutral recovery, non-humanoid torso-only mapping, and an opt-in confidence-colored debug puppet.
- Motion software gates: Node 376/376 PASS; asset safety 11/11 with 24 synchronized groups; signaling 13/13 PASS; existing Chromium regression 34/34 PASS; motion-focused Chromium 3/3 PASS; Android JVM 33/33 PASS; license audit PASS with the existing non-failing empty-author warning.
- Final motion APK: `android-capture/app/build/outputs/apk/debug/app-debug.apk`, 31,417,300 bytes, SHA256 `1593867200fd131816de32b09f4c5eb3f1ac599bce91afb948ec071691251bb1`.
- Device motion evidence: retained-data install PASS; default-off UI and persisted settings PASS; explicit local model load PASS; real-camera motion frames reached and were accepted by the device Receiver with renderer ready and zero page errors. Face plus pose used exactly one active CameraService client. Final low-profile sample was approximately 4.87 effective pose FPS with approximately 53.25 ms pose inference; active app PSS in a pose-only sample was 274,408 KiB.
- The unattended device scene contained no person. Tracking therefore remained `tracking-lost` with zero landmarks/confidence; this run does not claim human-body acquisition, neutral calibration completion, or quality accuracy. Deterministic simulator/browser tests cover tracked directions, crossed wrists, confidence, loss, and recovery.
- Final stop released the shared camera (zero active clients). Device logs contained zero FATAL, ANR, or pose Worker errors.

## Root cause and fix

- Previous run stopped because free space was below 2 GiB. Space is now 16 GiB; the hard stop is cleared.
- Asset sync review found that lexical path containment alone did not reject a symlink inside the repository that resolved outside it. Canonical-path and per-component symlink checks now reject that write before filesystem mutation.
- The old unit glob silently excluded `.mjs` tests. The new runner enumerates both `.test.js` and `.test.mjs`, while keeping asset and socket suites as explicit non-skipping gates.

## Gate status

- `ASSET_SYNC_GATE`: PASS
- `NODE_SIGNALING_GATE`: PASS
- `WEB_E2E_GATE`: PASS
- `ANDROID_BUILD_GATE`: PASS
- `DEVICE_CORE_GATE`: PASS (combined device and deterministic transport evidence; live human-face FPS not available because no face was present in the sampled scene)
- `MOTION_PROTOCOL_GATE`: PASS
- `MOTION_MODEL_GATE`: PASS
- `MOTION_DEVICE_GATE`: PASS WITH BOUNDARY (device inference/transport/lifecycle verified; live human-body acquisition unavailable in the unattended scene)

## Next step

Prepare the reviewed branch for both remotes and a draft pull request. Do not merge either default branch. A later attended device session may add human-body acquisition/calibration/quality evidence without reopening the completed protocol/model/lifecycle gates.
