# Motion Video Test Sources

The test videos are external local fixtures stored under `${CHEAPLIVE_MOTION_VIDEO_DIR}`. They are never committed, uploaded to CI artifacts, attached to releases, or copied into Android assets. Download and test execution are separate operations.

| ID | Source page | Creator | License | Download date | Local file name | SHA256 | Scene | Committed to Git |
|----|-------------|---------|---------|---------------|-----------------|--------|-------|------------------|
| Pending | Pending licensed source review | Pending | Pending | Pending | Pending | Pending | Pending | NO — LOCAL TEST DATA ONLY |

Replace the pending row only after the source page, official license page, creator, download size, and expected disk use have been reviewed. Record the stable source page URL rather than a temporary CDN URL. The committed inventory must not contain an absolute local path.
