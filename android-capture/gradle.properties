# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure the gradle daemon,
# see https://docs.gradle.org/current/userguide/gradle_daemon.html
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# NOTE: JDK 17 is required for Android Gradle Plugin 8.x.
# Set JAVA_HOME to your JDK 17 installation, or configure via ~/.gradle/gradle.properties:
#   org.gradle.java.home=/path/to/jdk17/Contents/Home
# When configured, incubating parallel mode may have issues.
# This option should only be used with decoupled projects.
# For more details, see https://developer.android.com/studio/build/gradle-multi-project-decoupled-project
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
